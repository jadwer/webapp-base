/**
 * Rutas estaticas publicas de ESTA app para el sitemap (id 0).
 * Cada tenant mantiene su propia lista: aqui solo lo que existe en
 * apps/template/src/app/(front). Las paginas del page builder y los
 * productos se agregan solos desde la API.
 */
export const SITEMAP_STATIC_PATHS = ['/', '/productos', '/ofertas', '/newsletter']
