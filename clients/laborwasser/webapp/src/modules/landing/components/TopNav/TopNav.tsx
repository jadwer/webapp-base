'use client'

/**
 * TopNav (DEPRECADO en el rediseno 2026-08).
 * El nav principal vive dentro de Header (un solo bloque blanco con dos
 * filas, como en el Figma). Este componente se conserva vacio para no romper
 * imports existentes; eliminar cuando el layout ya no lo referencie.
 */
export const TopNav: React.FC = () => null
